---
dependency:
  name: galaxy

driver:
  name: docker

provisioner:
  name: ansible
  # log: true

verifier:
  name: ansible

scenario:
  name: amazonlinux-1

platforms:
  - name: amazonlinux-1
    image: amazonlinux:1
