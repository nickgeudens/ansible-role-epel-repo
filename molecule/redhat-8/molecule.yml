---
dependency:
  name: galaxy

driver:
  name: podman

provisioner:
  name: ansible
  # log: true

verifier:
  name: ansible

scenario:
  name: redhat-8

platforms:
  - name: redhat-8
    image: registry.access.redhat.com/ubi8/ubi